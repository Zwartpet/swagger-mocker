specification:
    path:     /specification/{path}
    defaults: { _controller: api.document_controller:get }

default-yml:
    resource: "default.yml"
    type: swagger-mock
    prefix: yml/%mocker.prefix%

default-json:
    resource: "default.json"
    type: swagger-mock
    prefix: %mocker.prefix%
